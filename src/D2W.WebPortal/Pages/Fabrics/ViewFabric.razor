@page "/ViewFabric/{fabricId:guid}"

<MudText Typo="Typo.h5" Color="Color.Primary" Class="mb-4">@Resource.View_Fabric</MudText>

<ServerSideValidator @ref="ServerSideValidator" />
<MudCard>
    <MudCardContent>
        <MudTabs KeepPanelsAlive="true" Elevation="25" Color="Color.Transparent" Rounded="true" PanelClass="mt-6">
            <MudTabPanel Text="@Resource.Fabric" Icon="@Icons.Material.Filled.Person">
                <MudGrid>
                    <MudItem xs="6">
                        <MudTextField Disabled="true" Label="@Resource.Pattern" Variant="Variant.Outlined"
                            @bind-Value="FabricForEditVm.Pattern" For="@(() => FabricForEditVm.Pattern)" />

                        <MudTextField Disabled="true" Label="@Resource.Color" Variant="Variant.Outlined"
                            @bind-Value="FabricForEditVm.Color" For="@(() => FabricForEditVm.Color)" />
                    </MudItem>

                </MudGrid>
            </MudTabPanel>
        </MudTabs>
    </MudCardContent>
    <MudCardActions Class="pb-4 pl-4">
        <MudButton Link="/Fabrics" Variant="Variant.Filled" StartIcon="@Icons.Material.Outlined.ArrowBack"
            Color="Color.Primary">
            @Resource.Return
        </MudButton>
    </MudCardActions>
</MudCard>
